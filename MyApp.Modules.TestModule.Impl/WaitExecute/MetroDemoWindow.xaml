<controls:MetroWindow x:Class="MyApp.Modules.TestModule.Impl.WaitExecute.MetroDemoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        Title="MahApps.Metro Demo" Height="1200" Width="500"
        WindowStartupLocation="CenterScreen" Topmost="True"
        GlowBrush="{DynamicResource AccentColorBrush}">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="200"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Border Grid.Row="0" Background="{DynamicResource AccentColorBrush}" CornerRadius="4" Padding="12,8" Margin="0,0,0,12">
        <TextBlock Text="MahApps.Metro Sandbox" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
      </Border>
      <TextBox Grid.Row="1" controls:TextBoxHelper.Watermark="Текстовое поле" Margin="0,0,0,8" x:Name="MetroInput"/>
      <TextBox Grid.Row="2" controls:TextBoxHelper.Watermark="Ещё поле" Margin="0,0,0,8" x:Name="MetroInput2"/>
      <ComboBox Grid.Row="3" Margin="0,0,0,8" SelectedIndex="0">
        <ComboBoxItem Content="Вариант 1"/>
        <ComboBoxItem Content="Вариант 2"/>
        <ComboBoxItem Content="Вариант 3"/>
      </ComboBox>
      <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,8">
        <CheckBox Content="Чекбокс" IsChecked="True" Margin="0,0,16,0"/>
        <CheckBox Content="Ещё один"/>
      </StackPanel>
      <Slider Grid.Row="5" Minimum="0" Maximum="100" Value="50" Margin="0,0,0,8"/>
      <ProgressBar Grid.Row="6" Value="65" Minimum="0" Maximum="100" Height="8" Margin="0,0,0,12"/>
      <StackPanel Grid.Row="7" Orientation="Horizontal" VerticalAlignment="Top">
        <Button Content="Кнопка" Margin="0,0,8,0"/>
        <controls:ToggleSwitch Content="Переключатель" IsOn="True" Margin="0,0,8,0"/>
        <Button Content="Accent" Margin="0,0,8,0"/>
      </StackPanel>
      <DataGrid Grid.Row="8" x:Name="WallsDataGrid" ItemsSource="{Binding Walls}"
                AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                Margin="0,0,0,12" MinHeight="500">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="80"/>
          <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
        </DataGrid.Columns>
      </DataGrid>
      <Button Grid.Row="9" Content="Закрыть" Click="CloseButton_Click" HorizontalAlignment="Right" Margin="0,16,0,0"/>
    </Grid>
  </ScrollViewer>
</controls:MetroWindow>
